{{ 'overflow-list.css' | asset_url | preload_tag: as: 'style' }}
{{ 'base.css' | asset_url | stylesheet_tag: preload: true }}
{{ 'friday-storefront.css' | asset_url | stylesheet_tag }}
{{ 'friday-patches.css' | asset_url | stylesheet_tag }}
{{ 'friday-cart-overlay.css' | asset_url | stylesheet_tag }}
{{ 'friday-home-compression.css' | asset_url | stylesheet_tag }}
{{ 'friday-responsive-consistency.css' | asset_url | stylesheet_tag }}
{{ 'friday-hero-viewport-cap.css' | asset_url | stylesheet_tag }}

<style>
/* Friday Prints — World of GiGi v3
   Controlled exhibition layout. Inlined so Shopify cannot miss a newly-added asset. */

/* HERO: cleaner, less decorative, more confident. */
.fp-content-page--about .fp-page-hero{
  min-height:400px!important;
  padding-top:38px!important;
  padding-bottom:34px!important;
  grid-template-columns:minmax(0,1.2fr) minmax(300px,.8fr)!important;
}
.fp-content-page--about .fp-page-title{
  max-width:8.4ch!important;
  font-size:clamp(72px,7.7vw,112px)!important;
  text-transform:none!important;
}
.fp-content-page--about .fp-world__gigi{
  width:340px!important;
  height:300px!important;
  place-items:center!important;
}
.fp-content-page--about .fp-world__gigi::before{
  width:238px!important;
  height:270px!important;
  border-width:1px!important;
  transform:translate(-50%,-50%) rotate(-6deg)!important;
}
.fp-content-page--about .fp-world__gigi::after{display:none!important}
.fp-content-page--about .fp-world__gigi img{
  max-width:215px!important;
  max-height:255px!important;
  transform:none!important;
}

/* STORY: keep the dossier idea but reduce decoration. */
.fp-content-page--about .fp-world__story{
  padding-top:50px!important;
  padding-bottom:56px!important;
}
.fp-content-page--about .fp-world__story h2::after{margin-top:30px!important}
.fp-content-page--about .fp-world__copy p:last-child{
  font-size:34px!important;
  max-width:17ch!important;
}

/* ART EXHIBITION — deliberate 5-piece composition, not masonry. */
.fp-content-page--about .fp-world__gallery-head{
  padding:50px 46px 30px!important;
}
.fp-content-page--about .fp-world__gallery-head::before{
  content:"FRAGMENTS / 01—05"!important;
  top:24px!important;
}
.fp-content-page--about .fp-world__gallery-head h2{
  max-width:none!important;
  font-size:clamp(52px,5.4vw,76px)!important;
  white-space:nowrap;
}

.fp-content-page--about .fp-world__gallery{
  display:grid!important;
  columns:auto!important;
  grid-template-columns:repeat(12,minmax(0,1fr))!important;
  grid-auto-flow:row!important;
  grid-auto-rows:auto!important;
  column-gap:18px!important;
  row-gap:34px!important;
  align-items:start!important;
  padding:0 46px 58px!important;
  background:var(--fp-red)!important;
}
.fp-content-page--about .fp-world-card,
.fp-content-page--about .fp-world-card:nth-child(n){
  display:block!important;
  width:auto!important;
  height:auto!important;
  margin:0!important;
  break-inside:auto!important;
  align-self:start!important;
  overflow:hidden!important;
  background:transparent!important;
  border:0!important;
  outline:0!important;
}
.fp-content-page--about .fp-world-card:nth-child(1){grid-column:1 / 7!important;grid-row:1!important}
.fp-content-page--about .fp-world-card:nth-child(2){grid-column:8 / 12!important;grid-row:1!important;margin-top:92px!important}
.fp-content-page--about .fp-world-card:nth-child(3){grid-column:1 / 5!important;grid-row:2!important}
.fp-content-page--about .fp-world-card:nth-child(4){grid-column:5 / 9!important;grid-row:2!important;margin-top:44px!important}
.fp-content-page--about .fp-world-card:nth-child(5){grid-column:9 / 13!important;grid-row:2!important;margin-top:8px!important}
.fp-content-page--about .fp-world-card img{
  display:block!important;
  width:100%!important;
  height:auto!important;
  object-fit:contain!important;
  background:transparent!important;
  box-shadow:none!important;
}
.fp-content-page--about .fp-world-card::before{
  top:10px!important;
  left:10px!important;
  width:30px!important;
  height:30px!important;
  min-width:0!important;
  min-height:0!important;
  padding:0!important;
  border:1px solid rgba(23,18,15,.16);
  background:var(--fp-cream)!important;
  color:var(--fp-red)!important;
  font-size:8px!important;
}
.fp-content-page--about .fp-world-card::after{
  content:"OPEN ↗"!important;
  right:10px!important;
  bottom:10px!important;
  padding:8px 9px!important;
  background:var(--fp-cream)!important;
  opacity:0!important;
}
.fp-content-page--about .fp-world-card:hover::after{opacity:1!important}
.fp-content-page--about .fp-world-card:hover img{transform:scale(1.008)!important}

/* The sign-off is a separate calm beat after the exhibition. */
.fp-content-page--about .fp-world__gallery::after{
  content:"still wandering.  →"!important;
  grid-column:1 / -1!important;
  display:block!important;
  margin:18px -46px -58px!important;
  padding:30px 46px 34px!important;
  border-top:1px solid rgba(255,255,255,.45)!important;
  background:var(--fp-cream)!important;
  color:var(--fp-ink)!important;
  font:700 40px/.95 Georgia,"Times New Roman",serif!important;
  letter-spacing:-.04em!important;
}

/* TABLET: feature one artwork, then a disciplined 2x2 supporting set. */
@media (min-width:750px) and (max-width:1000px){
  .fp-content-page--about .fp-page-hero{
    min-height:370px!important;
    grid-template-columns:minmax(0,1.3fr) minmax(220px,.7fr)!important;
  }
  .fp-content-page--about .fp-world__gigi{width:245px!important;height:250px!important}
  .fp-content-page--about .fp-world__gigi::before{width:190px!important;height:220px!important}
  .fp-content-page--about .fp-world__gigi img{max-width:165px!important;max-height:200px!important}

  .fp-content-page--about .fp-world__gallery-head h2{white-space:normal;font-size:54px!important}
  .fp-content-page--about .fp-world__gallery{
    grid-template-columns:repeat(2,minmax(0,1fr))!important;
    gap:24px 14px!important;
    padding:0 30px 48px!important;
  }
  .fp-content-page--about .fp-world-card:nth-child(1){grid-column:1 / -1!important;grid-row:auto!important;width:64%!important;justify-self:center!important}
  .fp-content-page--about .fp-world-card:nth-child(2),
  .fp-content-page--about .fp-world-card:nth-child(3),
  .fp-content-page--about .fp-world-card:nth-child(4),
  .fp-content-page--about .fp-world-card:nth-child(5){grid-column:auto!important;grid-row:auto!important;margin-top:0!important;width:100%!important}
  .fp-content-page--about .fp-world__gallery::after{
    margin:14px -30px -48px!important;
    padding:27px 30px 30px!important;
    font-size:36px!important;
  }
}

/* MOBILE: don't miniaturize the art. Make it a swipeable zine rail. */
@media (max-width:749px){
  .fp-content-page--about .fp-page-hero{padding-bottom:4px!important}
  .fp-content-page--about .fp-world__gigi{height:205px!important;margin-top:5px!important}
  .fp-content-page--about .fp-world__gigi::before{width:168px!important;height:184px!important}
  .fp-content-page--about .fp-world__gigi img{max-width:140px!important;max-height:165px!important}

  .fp-content-page--about .fp-world__gallery-head{
    grid-template-columns:1fr!important;
    padding:38px 20px 20px!important;
  }
  .fp-content-page--about .fp-world__gallery-head h2{font-size:43px!important;white-space:normal}
  .fp-content-page--about .fp-world__gallery-head a{justify-self:start!important;margin-top:3px!important}

  .fp-content-page--about .fp-world__gallery{
    display:flex!important;
    columns:auto!important;
    gap:10px!important;
    overflow-x:auto!important;
    overflow-y:hidden!important;
    scroll-snap-type:x mandatory!important;
    -webkit-overflow-scrolling:touch;
    scrollbar-width:none;
    padding:0 20px 28px!important;
  }
  .fp-content-page--about .fp-world__gallery::-webkit-scrollbar{display:none}
  .fp-content-page--about .fp-world-card,
  .fp-content-page--about .fp-world-card:nth-child(n){
    flex:0 0 min(76vw,295px)!important;
    width:min(76vw,295px)!important;
    margin:0!important;
    grid-column:auto!important;
    grid-row:auto!important;
    scroll-snap-align:start!important;
  }
  .fp-content-page--about .fp-world-card::before{top:7px!important;left:7px!important;width:25px!important;height:25px!important}
  .fp-content-page--about .fp-world-card::after{display:none!important}
  .fp-content-page--about .fp-world__gallery::after{display:none!important}
}
</style>
